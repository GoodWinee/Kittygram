# Kittygram 🐱

![Workflow Status](https://github.com/GoodWinee/kittygram_final/actions/workflows/main.yml/badge.svg)

**Kittygram** — это современное веб-приложение для обмена фотографиями котиков. Проект позволяет пользователям загружать фото своих питомцев, просматривать ленту публикаций других пользователей и взаимодействовать с контентом.

## 📋 Описание проекта

Kittygram — это полноценное социальное приложение с разделением на бэкенд и фронтенд. Пользователи могут:

- 📸 Загружать фотографии своих котиков
- 👁️ Просматривать ленту публикаций
- 📝 Просматривать и редактировать свой профиль
- 🔐 Авторизовываться через токены

Проект разработан с использованием современных технологий и лучших практик разработки, включая контейнеризацию, CI/CD и автоматическое тестирование.

## 🛠️ Стек технологий

### Бэкенд
- **Python 3.12** — язык программирования
- **Django 3.2+** — веб-фреймворк
- **Django REST Framework** — создание REST API
- **Djoser** — аутентификация через токены
- **PostgreSQL 13** — база данных
- **Gunicorn** — WSGI-сервер

### Фронтенд
- **React** — библиотека для создания пользовательского интерфейса
- **JavaScript/TypeScript** — язык программирования
- **HTML5/CSS3** — разметка и стилизация

### Инфраструктура
- **Docker** — контейнеризация приложения
- **Docker Compose** — оркестрация контейнеров
- **Nginx** — веб-сервер и обратный прокси
- **GitHub Actions** — CI/CD пайплайн
- **Cerbot** — SSL сертификаты

## 🚀 Развертывание проекта

### Требования

- Docker 20.10+
- Docker Compose 2.0+
- Git
- Доменное имя (опционально для SSL)

### Быстрый старт

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/GoodWinee/Kittygram
   cd kittygram_final
2. **Создайте файл .env в корне проекта:**
   ```bash
   touch .env
3. **Заполните переменные окружения.**
   ```bash
   # Django Settings
   SECRET_KEY=Your_secret_key
   DEBUG=False
   ALLOWED_HOSTS=localhost,127.0.0.1
   # Database
   POSTGRES_DB=django
   POSTGRES_USER=django
   POSTGRES_PASSWORD=
   DB_HOST=db
   DB_PORT=5432
   # Optional: Telegram Notifications
   TELEGRAM_TOKEN=your-telegram-bot-token
   TELEGRAM_TO=your-telegram-chat-id
   
4. **Запустите проект:**
   ```bash
   docker compose -f docker-compose.production.yml up -d
5. **Примените миграции и соберите статику:**
   ```bash
   docker compose -f docker-compose.production.yml exec backend python manage.py migrate
   docker compose -f docker-compose.production.yml exec backend python manage.py collectstatic --noinput
6. **Откройте приложение в браузере:**
   - 🌐 Фронтенд: `http://localhost:9000`
   - 🔐 Админка: `http://localhost:9000/admin/`
   - 📡 API: `http://localhost:9000/api/`

## Автор

**GoodWinee** — разработчик проекта

- **GitHub:** [@GoodWinee](https://github.com/GoodWinee)
- **Репозиторий:** [https://github.com/GoodWinee/kittygram_final](https://github.com/GoodWinee/kittygram_final)
- **Демо:** [https://kiiittygram.ddns.net](https://kiiittygram.ddns.net)

---

**Счастливого кодинга! 🚀**
